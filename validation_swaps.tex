\documentclass[11pt]{article}

\usepackage[margin=1in]{geometry}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{amsmath,amssymb}
\usepackage{booktabs,tabularx,array}
\usepackage{graphicx}
\usepackage{float}
\usepackage{xcolor}
\usepackage{hyperref}
\usepackage{setspace}
\usepackage{caption}

\hypersetup{
  colorlinks=true,
  linkcolor=blue!50!black,
  citecolor=blue!50!black,
  urlcolor=blue!50!black
}
\setstretch{1.04}
\setlength{\parindent}{0pt}
\setlength{\parskip}{0.42em}
\captionsetup{font=small,labelfont=bf}
\newcolumntype{Y}{>{\raggedright\arraybackslash}X}

\newcommand{\E}{\mathbb{E}}
\newcommand{\V}{\mathbb{V}}
\newcommand{\1}{\mathbf{1}}
\input{generated/macros.tex}

\title{\textbf{Validation-Swap Distributions}\\[0.35em]
\large Diagnosing Machine-Scored Outcomes in Experiments}
\author{}
\date{August 2026}

\begin{document}
\maketitle

\begin{abstract}
Researchers increasingly use machine predictions or large language model scores
as experimental outcomes, then obtain human labels for a smaller validation
sample. Average prediction accuracy does not show whether scoring error distorts
the treatment contrast. We study the distribution of treatment-effect estimates
over fixed-size sets of clusters whose machine scores are replaced by human
labels. For a linear treatment contrast, the mean raw-swap path is mechanically
determined by its endpoints. Its dispersion and tails contain the information
about whether distortion is diffuse, concentrated, or offsetting. Scaling the
swap correction by its sampling probability yields a standard design-based
difference estimator, not a new correction method. The swap distribution instead
shows how the corrected estimate varies with the realized validation sample. In
an exactly enumerated experiment, two scoring systems have the same RMSE,
$R^2$, proxy effect, gold effect, and mean swap path, yet a five-cluster swap has
no effect \ConcentratedZeroMove\ of the time when scoring error is concentrated.
The diagnostic is conditional on the observed gold-label pool. Population
inference still requires a probability validation design or a transport model.
\end{abstract}

Researchers can score every response in an experiment with a classifier, a
computer-vision model, or a large language model while paying human coders to
score only a fraction. The cheap score may have excellent test-set accuracy and
still distort the treatment effect. The relevant error is not average scoring
error. It is the difference in scoring error between treatment arms.

Suppose an experiment tests whether a message changes the hostility expressed in
an open-ended response. A language model scores hostility for all respondents,
and human coders score a random subset. The model can overstate hostility by two
hundredths in one arm and understate it by two hundredths in the other. Its mean
error is zero, but the estimated treatment effect is wrong by four hundredths.
Aggregate accuracy conceals this error because accuracy and the treatment
contrast weight observations differently.

Existing methods already correct downstream inference with a small set of gold
labels. Post-prediction inference, design-based supervised learning, and
prediction-powered inference combine predictions on many observations with
gold-label residuals on a smaller probability sample
\cite{wang2020,egami2023,angelopoulos2023}. Two-phase validation designs study
how to select the expensive measurements efficiently \cite{amorim2021}. Work on
text as data explains why a learned outcome requires separate measurement and
causal design choices \cite{egami2022}. These methods establish the estimator
and its uncertainty.

This paper asks a different question: how much does the answer depend on which
clusters receive the gold labels? We define the fixed-budget random-swap
distribution of the treatment-effect estimate. Its mean is often uninformative.
Its spread shows whether a validation budget produces a stable correction or a
lottery. Its tails show how often the estimate moves by more than a prespecified
substantive amount. The same construction applies to people, documents, sites,
or other sampling units, but the swap unit must match the validation design and
the dependence structure.

The contribution is deliberately narrow. The design-scaled estimator below is a
special case of established difference and prediction-powered estimators. The
finite-population variance formula is standard sampling theory. We use these
results to define an estimand-specific validation diagnostic and to show why an
average swap path can hide the feature an audit is meant to find.

\section{Machine scores as experimental outcomes}

Index experimental clusters by $g=1,\ldots,G$ and observations within clusters
by $i$. Treatment $D_g\in\{0,1\}$ is assigned at the cluster level. Let $Y_{gi}$
be the human-coded outcome and $\widehat Y_{gi}$ the machine score. Define the
observed scoring discrepancy
\begin{equation}
e_{gi}=Y_{gi}-\widehat Y_{gi}.
\label{eq:error}
\end{equation}
The machine score must be fixed independently of the outcome analysis. A score
learned on the experimental outcomes requires a separate training sample or an
appropriate cross-fitting design. Swaps do not repair overfitting.

For clarity, consider the unadjusted difference in means. Write $N_1$ and $N_0$
for the numbers of observations in treated and control clusters. The proxy and
gold estimates are
\begin{align}
\widehat\tau_P
&=\frac{1}{N_1}\sum_{g:D_g=1}\sum_i\widehat Y_{gi}
-\frac{1}{N_0}\sum_{g:D_g=0}\sum_i\widehat Y_{gi},\\
\widehat\tau_G
&=\frac{1}{N_1}\sum_{g:D_g=1}\sum_iY_{gi}
-\frac{1}{N_0}\sum_{g:D_g=0}\sum_iY_{gi}.
\end{align}
Random treatment assignment gives these contrasts their experimental
interpretation. Validation sampling is a second design layered on top of the
experiment. It determines what can be learned about
$\widehat\tau_G-\widehat\tau_P$ when $Y$ is expensive.

Define cluster $g$'s contribution to this endpoint gap as
\begin{equation}
\psi_g=
\begin{cases}
N_1^{-1}\sum_i e_{gi}, & D_g=1,\\
-N_0^{-1}\sum_i e_{gi}, & D_g=0.
\end{cases}
\label{eq:psi}
\end{equation}
The contributions add exactly:
\begin{equation}
\sum_{g=1}^{G}\psi_g=\widehat\tau_G-\widehat\tau_P.
\label{eq:add}
\end{equation}
Equation~\eqref{eq:add} is an accounting identity within the paired gold-label
pool. It does not identify the errors of unlabeled clusters.
For OLS with fixed covariates, residualized-treatment weights replace the
treated and control weights in equation~\eqref{eq:psi}. The same adding-up and
sampling identities follow. A nonlinear estimator can still be recomputed over
swap sets, but its mean path need not be linear.

\section{The fixed-budget swap distribution}

Let $S_k$ be a simple random sample without replacement of exactly $k$ clusters.
The raw-swap estimate replaces machine scores by human outcomes in the selected
clusters:
\begin{equation}
\widehat\tau_R(S_k)=\widehat\tau_P+\sum_{g\in S_k}\psi_g.
\label{eq:raw}
\end{equation}
The distribution of $\widehat\tau_R(S_k)$ over all $\binom{G}{k}$ sets is the
raw fixed-budget swap distribution. It describes partial repair of the observed
data. A fixed $k$ is preferable to independent Bernoulli swaps when $k$
represents a coding budget.

Let $\bar\psi=G^{-1}\sum_g\psi_g$ and
$S_\psi^2=(G-1)^{-1}\sum_g(\psi_g-\bar\psi)^2$. Standard sampling identities
give
\begin{align}
\E_{S_k}[\widehat\tau_R(S_k)]
&=\widehat\tau_P+\frac{k}{G}
(\widehat\tau_G-\widehat\tau_P),
\label{eq:rawmean}\\
\V_{S_k}[\widehat\tau_R(S_k)]
&=k\left(1-\frac{k}{G}\right)S_\psi^2.
\label{eq:rawvar}
\end{align}

Equation~\eqref{eq:rawmean} is the central result. In a linear outcome analysis,
the average raw-swap path must be a straight line connecting the proxy and gold
estimates. Any two scoring systems with the same endpoints have the same mean
path. The path says nothing about whether the correction is spread evenly
across clusters or concentrated in a few. Equation~\eqref{eq:rawvar} shows where
that information resides.

The distribution supports several descriptive summaries. A researcher can
report its central quantiles, the chance that swapping $k$ clusters changes the
estimate's sign, and the chance that the movement exceeds a prespecified
substantive threshold. These probabilities refer to the stated swap design.
They are not $p$-values.

\section{From partial swaps to a validation estimator}

Raw replacement mechanically shrinks as $k/G$ falls. If the goal is to estimate
the fully human-coded contrast from a $k$-cluster probability sample, scale the
observed correction by its inclusion probability:
\begin{equation}
\widehat\tau_H(S_k)
=\widehat\tau_P+\frac{G}{k}\sum_{g\in S_k}\psi_g.
\label{eq:ht}
\end{equation}
This is a Horvitz-Thompson difference correction. In this simple setting it is
also the rectifier used by prediction-powered inference. It satisfies
\begin{align}
\E_{S_k}[\widehat\tau_H(S_k)]&=\widehat\tau_G,
\label{eq:htmean}\\
\V_{S_k}[\widehat\tau_H(S_k)]
&=\left(\frac{G}{k}\right)^2
\,k\left(1-\frac{k}{G}\right)S_\psi^2.
\label{eq:htvar}
\end{align}
Thus the design-scaled swap distribution is centered on the gold endpoint. Its
spread measures validation-sample fragility at budget $k$.

Unequal-probability validation replaces $G/k$ with inverse inclusion
probabilities. Stratifying the validation draw by treatment arm or by an
observable proxy-risk score can improve precision. Those choices change the
swap distribution and must be included in its definition. The optimal choice
depends on the target estimator \cite{amorim2021}.

An exact distribution requires $\psi_g$ for every cluster in the finite pool.
That is available in a benchmark dataset, a simulation, or a large pilot used to
study smaller future budgets. With only one small validation sample, the analyst
cannot recover the unseen cluster contributions nonparametrically. Standard
design-based variance estimation remains available when selection probabilities
are known. A cluster bootstrap or a model for $e_{gi}$ can approximate more of
the distribution, but then the result depends on resampling or transport
assumptions.

\subsection{Why this is not the wild cluster bootstrap}

The analogy to a wild cluster bootstrap is useful but incomplete. A wild
cluster bootstrap applies mean-zero random multipliers to cluster score
contributions to approximate a null sampling distribution
\cite{cameron2008}. A validation swap applies zero-one inclusion indicators to
observed gold-minus-proxy discrepancies. Its immediate target is the effect of a
labeling design, not repeated-sample uncertainty under a null.

For the linear contrast,
\[
\widehat\tau_R(S_k)-\E_{S_k}[\widehat\tau_R(S_k)]
=\sum_{g=1}^{G}\left(Z_g-\frac{k}{G}\right)\psi_g,
\]
where $Z_g=\1\{g\in S_k\}$. This centered expression has the form of a
finite-population multiplier process. The resemblance does not turn swap
quantiles into confidence intervals. Valid uncertainty for the experimental
effect must account for treatment assignment, and valid uncertainty for the
correction must account for the actual validation design.

\section{Same accuracy and endpoints, different swap distributions}

The numerical example has \NumClusters\ equal-size clusters with
\ClusterSize\ observations each. Half the clusters are treated. The fully
human-coded treatment effect is \GoldATE, and the machine-scored effect is
\ProxyATE\ in the diffuse and concentrated cases. All three scoring systems
have RMSE \PredictionRMSE\ and the same $R^2$ because they score the same gold
outcomes and have the same squared-error total.

The error patterns differ only in their cluster contributions. In the diffuse
case, each cluster contributes $\EndpointGap/\NumClusters$ to the endpoint gap.
In the concentrated case, two clusters contribute $\EndpointGap/2$ each and the
others contribute zero. The offsetting case has two positive and two negative
contributions of equal size, so the proxy and gold endpoints agree even though
individual swaps can move the estimate.

\begin{table}[H]
\centering
\caption{Matched prediction accuracy with different contribution patterns}
\label{tab:scenarios}
\small
\input{generated/table_scenarios.tex}

\vspace{0.35em}
\begin{minipage}{0.94\linewidth}
\footnotesize
\textit{Note:} Gold ATE and proxy ATE are differences in means. Gap is gold
minus proxy. Top-two share is the two largest absolute cluster contributions
divided by the sum of absolute contributions. Each row uses the same gold
outcomes. Quantities are deterministic for the generated finite population.
\end{minipage}
\end{table}

Figure~\ref{fig:same-mean} gives the comparison the mean path misses. Panel A
plots the exact mean over every fixed-size coalition. The diffuse and
concentrated paths coincide. Panel B displays the exact mass function for a
\BudgetExample-cluster raw swap. The diffuse correction always moves by the
same amount. The concentrated correction does not move
\ConcentratedZeroMove\ of the time because most coalitions contain neither
high-error cluster.

\begin{figure}[H]
\centering
\includegraphics[width=\linewidth]{generated/fig1_same_mean.png}
\caption{The mean swap path and exact fixed-budget distributions. Panel A shows
the mean raw-swap ATE over every set of a given size. The horizontal dotted line
is the gold ATE. Panel B shows the exact probability mass of the raw ATE movement
for every \BudgetExample-cluster subset. Both panels use the same
\NumClusters-cluster finite population.}
\label{fig:same-mean}
\end{figure}

\section{What a validation budget can deliver}

Figure~\ref{fig:budgets} shows the design-scaled estimator in
equation~\eqref{eq:ht}. The line is the median over all fixed-size validation
sets. Dark and light regions contain the middle 50 and 90 percent of the exact
distribution. Each distribution has mean \GoldATE\ by construction, but its
shape depends on the allocation of $\psi_g$.

The diffuse case is stable even with one labeled cluster because the example
assigns every cluster the same contribution. Concentrated error produces a
discrete and uneven distribution. Offsetting error is more revealing: the proxy
and gold endpoints are identical, yet small validation samples can imply large
positive or negative corrections. Endpoint agreement is therefore compatible
with fragile validation evidence.

\begin{figure}[H]
\centering
\includegraphics[width=\linewidth]{generated/fig2_budget_distributions.png}
\caption{Exact design-scaled swap distributions by validation budget. The solid
line is the median; dark and light regions contain the middle 50 and 90 percent
of all fixed-size validation sets. The dotted line is the fully gold-labeled ATE.
All panels share an axis.}
\label{fig:budgets}
\end{figure}

Table~\ref{tab:budgets} reports exact numerical summaries. Tail risk is the
share of validation sets whose scaled estimate differs from the gold endpoint
by more than \TailThreshold. Bias is omitted because it is zero up to numerical
precision for every cell. A narrow distribution does not prove that human
labels measure the intended construct. It says only that the specified
validation draw estimates the observed gold-label endpoint consistently.

\begin{table}[H]
\centering
\caption{Exact design-scaled distributions at selected budgets}
\label{tab:budgets}
\small
\input{generated/table_budgets.tex}

\vspace{0.35em}
\begin{minipage}{0.92\linewidth}
\footnotesize
\textit{Note:} Mean, SD, and quantiles describe equation~\eqref{eq:ht} over all
fixed-size cluster samples. Tail risk is
$\Pr(|\widehat\tau_H-\widehat\tau_G|>\TailThreshold)$. Enumeration is exact,
not Monte Carlo. Gold ATE equals \GoldATE\ in every scenario.
\end{minipage}
\end{table}

\section{Use and limits}

A validation-swap analysis begins with the same choices required by any
two-phase design. The researcher states the causal estimand, fixes the machine
scoring rule, identifies the unit sampled for human coding, and records every
cluster's inclusion probability. If treatment is assigned by cluster, swapping
individual labels while treating them as independent understates the relevant
variation.

The primary analysis should report a valid correction estimator and interval,
using prediction-powered, design-based supervised learning, or another method
whose assumptions fit the design. The swap distribution is an additional
diagnostic. Before seeing it, the researcher chooses validation budgets and a
substantively meaningful movement threshold. The exhibit then reports
quantiles, tail risk, and any decision reversal probability. Accuracy metrics
remain useful descriptions of the scoring model, but they do not substitute for
this coefficient-specific analysis.

The diagnostic has four limits. First, it cannot reveal the individual errors of
unlabeled observations. Second, a distribution computed inside a convenience
validation sample need not transport outside it. Third, human labels can contain
their own error or define the wrong construct. Fourth, a small number of
validated clusters may leave the distribution too poorly estimated to support
fine subgroup claims. The proper conclusion in that case is that the validation
budget does not resolve the scoring risk.

Machine-scored experiments create two sources of uncertainty: treatment
assignment and gold-label sampling. Randomization-based or cluster-robust
inference addresses the first. Established two-phase estimators address the
second when they correct the machine-scored contrast. The random-swap
distribution makes validation-sample dependence visible by showing how much the
conclusion depends on which clusters were checked.

\begin{thebibliography}{99}

\bibitem{amorim2021}
Gustavo Amorim, Ran Tao, Sarah Lotspeich, Pamela A. Shaw, Thomas Lumley, and
Bryan E. Shepherd. 2021.
Two-phase sampling designs for data validation in settings with covariate
measurement error and continuous outcome.
\emph{Journal of the Royal Statistical Society: Series A} 184(4):1368-1389.

\bibitem{angelopoulos2023}
Anastasios N. Angelopoulos, Stephen Bates, Clara Fannjiang, Michael I. Jordan,
and Tijana Zrnic. 2023.
Prediction-powered inference.
\emph{Science} 382(6671):669-674.

\bibitem{cameron2008}
A. Colin Cameron, Jonah B. Gelbach, and Douglas L. Miller. 2008.
Bootstrap-based improvements for inference with clustered errors.
\emph{Review of Economics and Statistics} 90(3):414-427.

\bibitem{egami2022}
Naoki Egami, Christian J. Fong, Justin Grimmer, Margaret E. Roberts, and
Brandon M. Stewart. 2022.
How to make causal inferences using texts.
\emph{Science Advances} 8(42):eabg2652.

\bibitem{egami2023}
Naoki Egami, Musashi Hinck, Brandon M. Stewart, and Hanying Wei. 2023.
Using imperfect surrogates for downstream inference: Design-based supervised
learning for social science applications of large language models.
\emph{Advances in Neural Information Processing Systems} 36.

\bibitem{wang2020}
Siruo Wang, Tyler H. McCormick, and Jeffrey T. Leek. 2020.
Methods for correcting inference based on outcomes predicted by machine
learning.
\emph{Proceedings of the National Academy of Sciences}
117(48):30266-30275.

\end{thebibliography}

\end{document}
